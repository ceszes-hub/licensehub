# Telep?t?s

Ubuntu Server 24.04, Docker Engine, Compose plugin, OpenSSL ?s curl sz?ks?ges. ?ll?tsa be a `TLS_SERVER_NAME` ?rt?k?t, majd futtassa: `sudo ./install.sh`. A script idempotens ?s a megl?v? `.env` f?jlt meg?rzi.
