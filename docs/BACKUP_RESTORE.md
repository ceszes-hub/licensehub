# Backup ?s restore

Automatikus custom-format dump ?s SHA-256 k?sz?l. Manu?lis ment?s: `./scripts/backup.sh`. Restore: `./scripts/restore.sh FILE.dump`; checksum ?s explicit RESTORE meger?s?t?s k?telez?.
